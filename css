/* Стили для заголовка */
.header {
  background-color: #222;
  color: #fff;
  text-align: center;
  padding: 20px;
}

/* Стили для основного контента */
.main {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
  background-color: #fff;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
}

/* Стили для изображения */
.image {
  text-align: center;
  margin-bottom: 20px;
}

.image img {
  max-width: 100%;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
}

/* Стили для описания */
.description {
  font-size: 20px;
  line-height: 1.5;
  margin-bottom: 20px;
}

/* Стили для списка артистов */
.artists {
  list-style: none;
  margin: 0;
  padding: 0;
  font-size: 18px;
  line-height: 1.5;
}

.artists li:before {
  content: "\2022";
  color: #222;
  display: inline-block;
  width: 1em;
  margin-left: -1em;
}

/* Стили для кнопки */
.button {
  display: inline-block;
  padding: 10px 20px;
  background-color: #222;
  color: #fff;
  text-decoration: none;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.button:hover {
  background-color: #444;
}
